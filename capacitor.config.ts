import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.nani.memo',
  appName: 'app-4',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
